server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: web-service-gateway

customer:
  service:
    url: ${GATEWAY_CUSTOMER_URL:localhost:8082}

eureka:
  instance:
    hostname: ${EUREKA_HOST:localhost}
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8081/eureka/
